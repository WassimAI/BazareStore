@model IEnumerable<BazarStore.Product>

@{int count = 0; }


@if (Model.Count() == 0)
{
    <p class="lead">No items found based on search criteria</p>
}

@foreach (var item in Model)
{
    if ((count % 4) == 0)
    {
        @:<div class="row">
        }
        <div class="col-md-3 col-sm-12 mb-4 mb-lg-2">
            <div class="card text-center">
                <img src="~/Images/Uploads/Products/@item.ProductID/@item.ImageUrl" class="card-img-top" alt="@item.ProductName">
                <div class="card-body">
                    <h5 class="card-title">@item.ProductName</h5>
                    <h6>$@item.UnitPrice</h6>
                    <a href="#" data-id="@item.ProductID" class="btn btn-primary">Add To Cart</a>
                </div>
            </div>
        </div>

        if (count == 3)
        {
            count = 0;
        @:</div>
    }
    else
    {
        count++;
    }

}
